// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebaseConfig : {
    apiKey: "AIzaSyBEpyvXPogLhDiRAquejABBrvpp8xRanrM",
    authDomain: "pcallr.firebaseapp.com",
    databaseURL: "https://pcallr.firebaseio.com",
    projectId: "pcallr",
    storageBucket: "pcallr.appspot.com",
    messagingSenderId: "634716247717",
    appId: "1:634716247717:web:8a5a4a55042a344c711f4c",
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
